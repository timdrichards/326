# Assignments

This directory is the source-of-truth home for homework and other assignment materials.

For homework, keep each assignment in its own folder under `assignments/homework/`.
Starter code and solution code live here, not inside the website publish tree.

The website build copies assignment specs into `website/docs/homework/` and zips starter code into `website/static/code/`.
