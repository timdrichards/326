# Lectures

This directory is the source-of-truth home for lecture materials.

Recommended lecture structure:

- `slides/`: Slidev deck source and deck-local assets
- `code/`: lecture demo/example code that will be zipped into `website/static/code/`

The website build copies or publishes lecture outputs into `website/docs/` and `website/static/`.
